import 'package:flutter/material.dart';

class SosIcon extends StatelessWidget {
  const SosIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/sos.png', width: 60, height: 60);
  }
}
