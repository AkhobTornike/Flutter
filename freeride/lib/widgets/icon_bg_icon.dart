import 'package:flutter/material.dart';

class IconBg extends StatelessWidget {
  const IconBg({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/icon_bg.png', width: 60, height: 60);
  }
}
