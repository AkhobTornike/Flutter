import 'package:flutter/material.dart';

class CompasIcon extends StatelessWidget {
  const CompasIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/compas.png', width: 15, height: 15);
  }
}
