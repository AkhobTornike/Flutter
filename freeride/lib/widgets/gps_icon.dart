import 'package:flutter/material.dart';

class GpsIcon extends StatelessWidget {
  const GpsIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/gps.png', width: 20, height: 20);
  }
}
