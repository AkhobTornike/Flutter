import 'package:flutter/material.dart';

class MountainIcon extends StatelessWidget {
  const MountainIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/mountain.png', width: 20, height: 20);
  }
}
